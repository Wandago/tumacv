import LoginClient from "./LoginClient";

export const metadata = {
  title: "Sign In — TumaCV",
  description: "Sign in or create a free TumaCV account — 5 free tailored applications, no card required.",
};

export default function Page() {
  return <LoginClient />;
}
